<h1> Search All Assemblies</h1>
<%= form_tag reports_search_path, method: :get do %>
  <%= text_field_tag :search, params[:search] %>
  <%= submit_tag "Search" %>
<% end %>

<% if params[:search] %>
  <h1>Search Results</h1>

  <table>
    <thead>
      <tr>
        <th>Assembly Name</th>
        <th>Gene DNA</th>
        <th>Hit Match Gene Name</th>
      </tr>
    </thead>
    <tbody>
      <% @assemblies.each do |a| %>
        <tr>
          <td><%= a.name %></td>
        </tr>
      <% end %>
    </tbody>
  </table>

<% end %>
